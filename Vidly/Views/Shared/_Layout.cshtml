@*If we want to add a model, this is how we do it*@

@*@model Vidly.Models.ViewModels.RandomMovieViewModel*@

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    @*If we do not pass a model in the below line, the model which we have referred above would be automatically passed in the below partial view. But if this NavBar expects a Movie not the entire RandomMovieViewModel*@
    @* @Html.Partial("_NavBar", Model.Movie)*@
    @Html.Partial("_NavBar")
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Vidly</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/lib")
    @RenderSection("scripts", required: false)
</body>
</html>

